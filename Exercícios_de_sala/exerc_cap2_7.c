#include <stdio.h> 
int main ()
{ 
  int numero;
printf("Informe o número:");
scanf("%d", &numero);

  if (numero >= 20 && numero <= 50)
printf("Este número está entre 20 e 50");
  else 
printf("Este número não está entre 20 e 50");

}	  
